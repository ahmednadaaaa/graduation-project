// Re-export everything from the unified data file
// @/types/data.ts is kept for backward compatibility
export type {
  AdminStudent as Student,
  AdminDriver as Driver,
  AdminBus as Bus,
  AdminRoute as Route,
} from "@/utils/data";
